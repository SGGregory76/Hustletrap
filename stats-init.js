let playerCash = 100;
let playerInventory = ["weed", "cigs"];

function logEvent(text) {
  console.log("[LOG]", text);
  // optional: write to DOM log element or localStorage
}

function updateUI() {
  // Refresh stats display, inventory list, etc.
  // You’d hook this into your Blogger layout
}
